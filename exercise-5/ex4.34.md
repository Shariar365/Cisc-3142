>(a) fval is converted to bool. 
>(b) ival is converted to float, then it is added to fval, the value is converted to double. 
>(c) cval is converted to int, then multiplied by ival, then the value is converted to double, then added to dval.